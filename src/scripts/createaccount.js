
function armazenar (){
    email = document.areaDeLogin.email.value
    user = document.areaDeLogin.usuario.value
    senha = document.areaDeLogin.senha.value

    const dadosusers = {
        email: email,
        usuario: user,
        senha: senha
    }

    const registros = [dadosusers]
}